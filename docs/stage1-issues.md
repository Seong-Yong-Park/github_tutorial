# Stage 1 — 등록할 이슈 10개

GitHub 웹 UI에서 **직접 하나씩** 만드세요. CLI는 Stage 1을 마친 뒤
`_answers/stage1/seed_issues.sh`로 비교해봅니다.

각 이슈에 라벨을 붙이고, 전부 Project 보드에 올립니다.

---

### #1 라이다 드라이버 노드 스켈레톤 작성
`feature`

RPLIDAR A1을 읽어 `/scan` 토픽으로 퍼블리시하는 노드의 뼈대를 만든다.
파라미터: 포트, 보드레이트, 프레임 ID.

### #2 라이다 최근접 거리가 간헐적으로 NaN으로 들어옴
`bug`

**재현**: 실내 유리벽 근처에서 5분 이상 주행
**현상**: `safety.should_stop()`이 NaN을 받으면 항상 False를 반환해서 정지하지 않음
**기대**: 센서 값이 비정상이면 안전 측으로(정지) 판단해야 함

> Stage 2에서 이 이슈를 실제로 고칩니다. `src/mini_delivery_bot/safety.py` 참고.

### #3 README에 빌드 방법 추가
`chore`

신규 팀원이 clone 후 5분 안에 테스트를 돌릴 수 있어야 한다.

### #4 코너 진입 시 감속이 너무 늦음
`bug`

곡률 반경 0.5m 이하 코너에서 감속 시작이 늦어 벽에 근접함.

### #5 배터리 잔량 토픽 퍼블리시
`feature`

`/battery_state`로 SOC와 예상 잔여 주행 시간을 퍼블리시한다.

### #6 Docker 개발 이미지 만들기
`chore`

로컬 / CI / 타깃이 동일한 이미지를 쓰도록 한다.

### #7 장애물 회피 파라미터 튜닝
`feature`

인플레이션 반경과 코스트 스케일링을 실내 환경에 맞게 조정.

### #8 로그가 너무 많이 찍혀서 디스크가 참
`bug`

주행 1시간에 2GB. 스로틀링 적용 필요.

### #9 시뮬레이션 월드 파일 추가
`feature`

사무실 레이아웃을 본뜬 Gazebo 월드 1개.

### #10 나중에 - 웹 대시보드 있으면 좋을 듯
`idea`

당장은 안 함. 백로그에만 둔다.
